1、golang中make和new的区别
    * 共同：为变量分配内存
    * 区别：作用变量类型不同，new给string、int、数组分配内存，make给切片、channel、map分配内存
           返回类型不一样，new返回指向变量的指针，make返回变量本身

2、数组和切片的区别
    相同:
    * 都是保存相同类型数据
    * 都是通过下标来访问

    区别:
    * 数组是定长的，切片可以自动扩容
    * 数组是值类型，切片是引用类型

